import { Hex } from 'viem';
import type { Account, AccountContract, CreateSessionKeyOptions, FeeQuotesResponse, IEthereumProvider, PasskeyProvider, RequestArguments, SendTransactionParams, SessionKey, SessionKeySignerParams, SmartAccountConfig, Transaction, UserOp, UserOpBundle, UserOpParams } from './types';
export declare class SmartAccount {
    provider: IEthereumProvider & Partial<PasskeyProvider>;
    private config;
    private connection;
    private smartAccountContract;
    constructor(provider: IEthereumProvider & Partial<PasskeyProvider>, config: SmartAccountConfig);
    setSmartAccountContract(contract: AccountContract): void;
    getChainId: () => Promise<string>;
    getOwner: () => Promise<string>;
    signUserOpHash: (userOpHash: Hex) => Promise<string>;
    private getAccountConfig;
    getFeeQuotes(tx: Transaction | Transaction[]): Promise<FeeQuotesResponse>;
    buildUserOperation({ tx, feeQuote, tokenPaymasterAddress }: UserOpParams): Promise<UserOpBundle>;
    signUserOperation({ userOpHash, userOp }: UserOpBundle): Promise<UserOp>;
    sendUserOperation({ userOpHash, userOp }: UserOpBundle): Promise<string>;
    sendSignedUserOperation(userOp: UserOp, signerParams?: SessionKeySignerParams): Promise<string>;
    sendTransaction(params: SendTransactionParams): Promise<string>;
    getAccount(): Promise<Account>;
    getAddress(): Promise<string>;
    isDeployed(): Promise<boolean>;
    deployWalletContract(): Promise<string>;
    sendRpc<T>(arg: RequestArguments): Promise<T>;
    createSessions(options: CreateSessionKeyOptions[]): Promise<FeeQuotesResponse>;
    validateSession(targetSession: SessionKey, sessions: SessionKey[]): Promise<boolean>;
}
