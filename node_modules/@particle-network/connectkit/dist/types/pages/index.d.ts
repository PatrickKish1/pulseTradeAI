import type { FC } from 'react';
import type { TransactionKey } from '../locales';
export declare enum RoutePath {
    Signup = "signup",
    Connectors = "connectors",
    Captcha = "captcha",
    Connecting = "connecting",
    DownloadApp = "download-app",
    SelectChain = "select-chain",
    Walletconnect = "walletconnect",
    WalletInfo = "wallet-info",
    Receive = "receive",
    PasskeySetup = "passkey-setup",
    LoginPasskey = "login-passkey"
}
export declare const safeRoutes: {
    connected: RoutePath[];
    disconnected: RoutePath[];
};
export declare const allRoutes: readonly RoutePath[];
export interface IPage {
    title?: TransactionKey | FC;
    path: RoutePath;
    element: FC<any>;
}
export declare const pages: IPage[];
export declare const getPage: (path: RoutePath) => IPage;
