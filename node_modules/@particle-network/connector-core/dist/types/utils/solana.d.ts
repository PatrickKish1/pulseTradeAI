import type { Connection, SendOptions, Transaction, VersionedTransaction } from '@solana/web3.js';
import type { SolanaProvider } from '../types/solana';
export declare function isVersionedTransaction(transaction: Transaction | VersionedTransaction): transaction is VersionedTransaction;
export declare const sendSolanaTransaction: (provider: SolanaProvider, transaction: Transaction | VersionedTransaction, connection: Connection, options?: SendOptions) => Promise<{
    signature: string;
}>;
