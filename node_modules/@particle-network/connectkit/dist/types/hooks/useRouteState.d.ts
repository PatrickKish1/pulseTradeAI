import type { Route } from '../context/types';
import { RoutePath } from '../pages';
type State = {
    route: Route;
};
type Actions = {
    setRoute: (value: Route | RoutePath) => void;
};
export declare const useRouteState: import("zustand").UseBoundStore<import("zustand").StoreApi<State & Actions>>;
export {};
