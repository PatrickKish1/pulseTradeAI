import { type SolanaProvider } from '@particle-network/connector-core';
import type { SolanaWallet } from '@particle-network/wallet';
export declare const createSolanaWallet: (provider: SolanaProvider, chainId: number) => SolanaWallet & {
    provider: SolanaProvider;
};
