import { type WalletProps } from '../../hooks/useWalletsProps';
export type WalletData = {
    key: string;
    lable?: string;
    customLable?: boolean;
    props: [WalletProps, ...WalletProps[]];
    rankingWeight: number;
};
